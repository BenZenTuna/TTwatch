services:
  # Embedder runs on CPU to leave GPU for both vLLM instances.
  # Qwen3-Embedding-0.6B is tiny (0.6B params) and performs well on CPU.
  embedder:
    build: ./services/embedder
    restart: unless-stopped
    ports: ["8101:8001"]
    environment:
      MODEL_NAME: "${EMBEDDING_MODEL_NAME:-Qwen/Qwen3-Embedding-0.6B}"
      EMBEDDER_DEVICE: "cpu"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8001/health"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 60s

  # Primary reasoning model — loads FIRST on empty GPU.
  # 0.65 * 32607 = ~21 GB budget. Model weights ~16.5 GB, leaves ~4.5 GB for KV cache.
  vllm:
    image: vllm/vllm-openai:${VLLM_VERSION:-v0.16.0}
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices: [{capabilities: [gpu]}]
    volumes: [./models:/models]
    command: >
      --model /models/${LOCAL_MODEL_NAME:-Qwen3-32B-AWQ}
      --quantization awq_marlin
      --gpu-memory-utilization 0.65
      --max-model-len 8192
      --max-num-seqs 8
      --enable-prefix-caching
      --reasoning-parser deepseek_r1
      --port 8000
    ports: ["8100:8000"]
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 120s

  # Fast classification model — loads SECOND after vllm is healthy.
  # 0.85 util so cache budget = total*0.85 - all_used > 0 despite vllm's allocation.
  vllm-fast:
    image: vllm/vllm-openai:${VLLM_VERSION:-v0.16.0}
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices: [{capabilities: [gpu]}]
    volumes: [./models:/models]
    command: >
      --model /models/${FAST_MODEL_NAME:-Qwen3-8B-AWQ}
      --quantization awq_marlin
      --gpu-memory-utilization 0.85
      --max-model-len 8192
      --max-num-seqs 16
      --enable-prefix-caching
      --disable-log-requests
      --port 8000
    ports: ["8102:8000"]
    depends_on:
      vllm: { condition: service_healthy }
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 120s

  # Override dependencies to include GPU services
  api:
    depends_on:
      postgres: { condition: service_healthy }
      qdrant: { condition: service_healthy }
      redis: { condition: service_healthy }
      minio: { condition: service_healthy }
      vllm: { condition: service_healthy }
      vllm-fast: { condition: service_healthy }
      embedder: { condition: service_healthy }

  worker-io:
    depends_on:
      postgres: { condition: service_healthy }
      qdrant: { condition: service_healthy }
      redis: { condition: service_healthy }
      minio: { condition: service_healthy }
      vllm: { condition: service_healthy }
      vllm-fast: { condition: service_healthy }
      embedder: { condition: service_healthy }

  worker-cpu:
    depends_on:
      postgres: { condition: service_healthy }
      qdrant: { condition: service_healthy }
      redis: { condition: service_healthy }
      minio: { condition: service_healthy }
      vllm: { condition: service_healthy }
      vllm-fast: { condition: service_healthy }
      embedder: { condition: service_healthy }
