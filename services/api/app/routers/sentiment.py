"""Sentiment history and analysis endpoints."""
from fastapi import APIRouter

router = APIRouter()

# TODO: GET /topics/{topic_id}/sentiment — sentiment history time series
# TODO: GET /clusters/{cluster_id}/sentiment — cluster-level sentiment history
