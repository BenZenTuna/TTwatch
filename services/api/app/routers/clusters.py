"""Cluster management endpoints."""
from fastapi import APIRouter

router = APIRouter()

# TODO: GET /clusters/{cluster_id} — get single cluster with articles
# TODO: GET /clusters/{cluster_id}/articles — list articles in cluster
# TODO: POST /clusters/{cluster_id}/summarize — trigger cluster summarization
