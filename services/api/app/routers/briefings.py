"""Briefing generation and retrieval endpoints."""
from fastapi import APIRouter

router = APIRouter()

# TODO: GET /topics/{topic_id}/briefings — list briefings for a topic
# TODO: GET /briefings/{briefing_id} — get single briefing
# TODO: POST /topics/{topic_id}/briefings — trigger briefing generation
